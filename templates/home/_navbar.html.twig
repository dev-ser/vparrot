{% block header %}

	<!-- Navbar Start -->
	<div class="contaner-fluid">
	<nav class="navbar navbar-expand-lg bg-white navbar-light shadow sticky-top p-0 m-0">
    <a href="" class="navbar-brand d-flex align-items-center px-4 px-lg-5">
			<img src="/assets/img/logo.png" class="m-0 text-primary w-25" alt="">
		</a>
   <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
		<div class="collapse navbar-collapse" id="navbarCollapse">
			<div class="navbar-nav ms-auto p-4 p-lg-0 m-3">
					<a class="nav-item nav-link active" aria-current="page" href="{{ path('app_home')}}">Accueil</a>
					<div class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Services</a>
						<div class="dropdown-menu fade-up m-0">
							<a class="dropdown-item" aria-current="page" href="{{ url('app_atelier') }}">Carrosserie</a>
							<a class="dropdown-item" aria-current="page" href="{{ url('app_atelier') }}">Mécanique</a>
							<a class="dropdown-item" aria-current="page" href="{{ url('app_atelier') }}">Entretien</a>
						</div>
					</div>
					<a class="nav-item nav-link" aria-current="page" href="{{ url('app_voitures_index') }}">Voitures</a>
					<a class="nav-item nav-link" aria-current="page" href="{{ url('app_avis') }}">Avis</a>
					<a class="nav-item nav-link" aria-current="page" href="{{ path('app_contact')}}">Contact</a>

				 {# <div class="d-flex px-2">
                    <a href="{{ path('app_login') }}" class="btn btn-danger py-2 px-lg-3 me-5" type="submit">Connexion<i class="fa fa-arrow-right ms-3"></i></a>
                </div> #}
				 <div class="d-flex px-2">
				{% if app.user %}
    {# Si l'utilisateur est connecté, affiche le bouton de déconnexion #}
    <a href="{{ path('app_logout') }}" class="btn btn-danger py-2 px-lg-3 me-5" type="submit">Deconnexion<i class="fa fa-arrow-right ms-3"></i></a>
{% else %}
    {# Si l'utilisateur n'est pas connecté, affiche le bouton de connexion #}
	<a href="{{ path('app_login') }}" class="btn btn-danger py-2 px-lg-3 me-5" type="submit">Connexion<i class="fa fa-arrow-right ms-3"></i></a>
{% endif %}
			</div>
			</div>
			</div>
		</nav>
	{% endblock %}
